diff --git a/docker-compose.yml b/docker-compose.yml
index 8873990..d2a8777 100644
--- a/docker-compose.yml
+++ b/docker-compose.yml
@@ -38,6 +38,8 @@ x-sentry-defaults: &sentry_defaults
       <<: *depends_on-healthy
     postgres:
       <<: *depends_on-healthy
+    pgbouncer:
+      <<: *depends_on-default
     memcached:
       <<: *depends_on-default
     smtp:
@@ -144,6 +146,19 @@ services:
       POSTGRES_HOST_AUTH_METHOD: "trust"
     volumes:
       - "sentry-postgres:/var/lib/postgresql/data"
+  pgbouncer:
+    image: edoburu/pgbouncer
+    environment:
+      - DB_USER=${POSTGRES_USER:-postgres}
+      - DB_PASSWORD=${POSTGRES_PASSWORD:-sentry}
+      - DB_HOST=postgres
+      - POOL_MODE=transaction
+      - ADMIN_USERS=postgres,sentry
+      - MAX_CLIENT_CONN=10000
+    ports:
+      - "5432:5432"
+    depends_on:
+      - postgres
   kafka:
     <<: *restart_policy
     image: "confluentinc/cp-kafka:7.6.1"
